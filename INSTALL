1. Download the zip file (bayesmith-artifact.zip) from the following url: https://doi.org/10.5281/zenodo.5324064

2. Unzip the file and obtain the tarball (bayesmith.tar.gz) that contains Docker image.

3. Load the docker image:

  $ [sudo] docker load < bayesmith.tar.gz
